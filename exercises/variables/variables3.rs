// variables3.rs
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a hint.

// variables that are being used must be initialized
fn main() {
    let x: i32=1;
    println!("Number {}", x);
}
