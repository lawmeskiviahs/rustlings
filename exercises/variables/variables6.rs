// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

// for a constant to be declared we need to define a data-type beforeahand
const NUMBER: u8 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
