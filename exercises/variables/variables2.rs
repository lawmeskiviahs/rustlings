// variables2.rs
// Execute `rustlings hint variables2` or use the `hint` watch subcommand for a hint.

// variables that are to be used must be initialized beforehand
fn main() {
    let x=10;
    // let x = 0;
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
